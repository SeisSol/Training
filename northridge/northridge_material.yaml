!Switch
[rho, mu, lambda]: !Include northridge_rho_mu_lambda.yaml
[Qp, Qs]: !EvalModel
    parameters: [rho, mu, lambda]
    model: !Switch
        [rho, mu, lambda]: !Include northridge_rho_mu_lambda.yaml
    components: !FunctionMap
        map:
            Qs: return 0.025*sqrt(mu/rho);
            Qp: return 0.05*sqrt(mu/rho);
